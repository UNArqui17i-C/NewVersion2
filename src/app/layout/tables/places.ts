export class Places{
    email: string;
    latitude : string;
    length: string;

    constructor( email:string, latitude: string, length: string ){
        this.email = email;
        this.latitude = latitude ;
        this.length = length;
    }
}
